apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "manifestKubernete.fullname" . }}-test-connection"
  labels:
    {{- include "manifestKubernete.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "manifestKubernete.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
