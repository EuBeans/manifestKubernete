apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "help-repo.fullname" . }}-test-connection"
  labels:
    {{- include "help-repo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "help-repo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
